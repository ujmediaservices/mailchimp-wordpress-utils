__pycache__/
*.pyc
*.pyo
.venv/
.env
.claude/settings.local.json
.claude/skills/send-free-newsletter/last-posts.json
.claude/skills/send-insider-newsletter/insider-segment.json
tmp/

# Scratch / one-off analysis
.tmp-*

# Local data dumps — regenerated on demand, never committed
# (inserts/ is intentionally tracked: reusable newsletter-insert assets)
data/

# Per-week JP-social screenshots + the canonical editor's-note file
# (archived copies under inserts/archive/ also never committed).
# The denylist file jp_social_news_handles.txt IS tracked.
inserts/jp-social-*/
inserts/jp-social-*.md
inserts/extras-*.md
inserts/editors-notes.md
inserts/archive/

